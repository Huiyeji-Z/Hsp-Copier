<Window x:Class="HspCopier.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:clipboard="clr-namespace:HspCopier.Core.Clipboard;assembly=HspCopier.Core"
        Title="Hsp Copier"
        Height="500" Width="320"
        MinHeight="300" MinWidth="240"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        UseLayoutRounding="True"
        WindowStartupLocation="Manual"
        Loaded="MainWindow_OnLoaded"
        LocationChanged="MainWindow_OnLocationChanged"
        SizeChanged="MainWindow_OnSizeChanged"
        MouseLeftButtonDown="MainWindow_OnMouseLeftButtonDown"
        MouseMove="MainWindow_OnMouseMove"
        MouseLeftButtonUp="MainWindow_OnMouseLeftButtonUp"
        MouseLeave="MainWindow_OnMouseLeave">
    <Border x:Name="RootBorder"
            CornerRadius="14"
            BorderThickness="0"
            Padding="0">
        <Border.Background>
            <SolidColorBrush x:Name="RootBackgroundBrush" Color="#1E1E2E" />
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="0"
                              Opacity="0.55" Color="Black"/>
        </Border.Effect>
        <Border.Clip>
            <RectangleGeometry x:Name="RootClip" RadiusX="14" RadiusY="14"/>
        </Border.Clip>
        <Grid>
            <Grid.Resources>
                <!-- 美化列表滚动条：细、半透明、圆角，悬停才显色 -->
                <Style TargetType="ScrollBar">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#55FFFFFF"/>
                    <Setter Property="Width" Value="6"/>
                    <Setter Property="MinWidth" Value="6"/>
                    <Setter Property="Margin" Value="0,2,2,2"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid Background="Transparent">
                                    <Border x:Name="TrackBackground"
                                            Background="Transparent"
                                            CornerRadius="3"
                                            Margin="0"/>
                                    <Track x:Name="PART_Track" IsDirectionReversed="True">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Command="ScrollBar.PageUpCommand" Style="{x:Null}">
                                                <RepeatButton.Template>
                                                    <ControlTemplate TargetType="RepeatButton">
                                                        <Border Background="Transparent"/>
                                                    </ControlTemplate>
                                                </RepeatButton.Template>
                                            </RepeatButton>
                                        </Track.DecreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb>
                                                <Thumb.Template>
                                                    <ControlTemplate TargetType="Thumb">
                                                        <Border x:Name="ThumbBg"
                                                                Background="#33FFFFFF"
                                                                CornerRadius="3"
                                                                Margin="0"/>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="ThumbBg" Property="Background" Value="#AAFFFFFF"/>
                                                            </Trigger>
                                                            <Trigger Property="IsDragging" Value="True">
                                                                <Setter TargetName="ThumbBg" Property="Background" Value="#CCFFFFFF"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Thumb.Template>
                                            </Thumb>
                                        </Track.Thumb>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Command="ScrollBar.PageDownCommand" Style="{x:Null}">
                                                <RepeatButton.Template>
                                                    <ControlTemplate TargetType="RepeatButton">
                                                        <Border Background="Transparent"/>
                                                    </ControlTemplate>
                                                </RepeatButton.Template>
                                            </RepeatButton>
                                        </Track.IncreaseRepeatButton>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Grid Grid.Row="0" Height="36" Background="#22FFFFFF"
                  MouseLeftButtonDown="TitleBar_OnMouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Hsp Copier"
                           VerticalAlignment="Center" Margin="12,0,0,0"
                           FontWeight="SemiBold" Foreground="White"/>
                <Button Grid.Column="1" x:Name="PinButton"
                        Content="📌" Width="32" Height="28"
                        Background="Transparent" Foreground="White"
                        BorderThickness="0" Click="PinButton_Click"
                        ToolTip="固定 / 取消固定（固定后强制不变形）"/>
                <Button Grid.Column="2" x:Name="SettingsButton"
                        Content="⚙" Width="32" Height="28"
                        Background="Transparent" Foreground="White"
                        BorderThickness="0" Click="SettingsButton_Click"
                        ToolTip="设置"/>
                <Button Grid.Column="3" x:Name="CloseButton"
                        Content="✕" Width="32" Height="28"
                        Background="Transparent" Foreground="White"
                        BorderThickness="0" Click="CloseButton_Click"
                        FontSize="13"
                        ToolTip="再次点击关闭应用"/>
            </Grid>

            <!-- 历史记录列表 -->
            <ListBox Grid.Row="1" x:Name="HistoryList"
                     Background="Transparent" BorderThickness="0"
                     Foreground="White"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseLeftButtonUp="HistoryList_OnMouseLeftButtonUp">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#33FFFFFF"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#22FFFFFF"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#3A6C5CE7"/>
                                            <Setter Property="BorderBrush" Value="#556C5CE7"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <!-- 关闭提示浮动层 -->
            <Border Grid.Row="1" x:Name="CloseHintBorder"
                    HorizontalAlignment="Center" VerticalAlignment="Bottom"
                    Margin="0,0,0,20"
                    Background="#E6000000" CornerRadius="8"
                    Padding="16,8" Visibility="Collapsed"
                    Opacity="0">
                <TextBlock Text="再次点击 ✕ 关闭应用"
                           Foreground="White" FontSize="12"/>
            </Border>

            <!-- 美化的缩放手柄 -->
            <Thumb Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Width="18" Height="18" Cursor="SizeNWSE"
                   DragDelta="ResizeThumb_OnDragDelta"
                   ToolTip="拖动调整大小">
                <Thumb.Template>
                    <ControlTemplate TargetType="Thumb">
                        <Grid Background="Transparent">
                            <Path Stroke="#66FFFFFF" StrokeThickness="1.5"
                                  Data="M 3,15 L 15,3 M 7,15 L 15,7 M 11,15 L 15,11"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                  Margin="0,0,3,3"/>
                        </Grid>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
        </Grid>
    </Border>
</Window>
